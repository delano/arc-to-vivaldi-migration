node_modules/

# Generated bookmark/page output — embeds real URLs + titles; never commit
arc-*.html
arc-*.json
sparca.html
sparca.json
split/

# Browser-verification screenshots capture the rendered page, so they embed real
# tab titles + URLs; never commit them.
.shots/

# Generated DevTools scripts and probe captures — they embed real tab URLs,
# workspace names, and other personal browsing data; never commit them.
vivaldi-import.js
vivaldi-unwind.js
probe-vivaldi.js
probe-vivaldi-consoleoutput.json

# A real StorableSidebar.json contains personal browsing data — never commit it
StorableSidebar.json

*.log
.DS_Store
Thumbs.db
